# NixOS systemd service module for Frappe bench production deployment.
# Placeholder — future work.
{ lib, ... }:
{
  # Options and config for NixOS-native Frappe deployment will go here.
  # Planned: systemd services for gunicorn, workers, scheduler;
  # NixOS module options for site config, Redis, MariaDB integration;
  # secrets management integration.
}
